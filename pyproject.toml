[project]
name = "wealth-decoded-ai"
version = "0.1.0"
description = "Backend infrastructure for Wealth Decoded AI."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.30.0,<1.0.0",
    "elevenlabs>=2.0.0,<3.0.0",
    "fastapi>=0.115.0,<1.0.0",
    "loguru>=0.7.2,<1.0.0",
    "openai>=1.0.0,<3.0.0",
    "pillow>=11.0.0,<12.0.0",
    "pydantic-settings>=2.6.0,<3.0.0",
    "sqlalchemy[asyncio]>=2.0.36,<3.0.0",
    "uvicorn[standard]>=0.32.0,<1.0.0",
]

[dependency-groups]
dev = [
    "black>=24.10.0,<26.0.0",
    "httpx>=0.28.0,<1.0.0",
    "mypy>=1.13.0,<2.0.0",
    "pytest>=8.3.0,<9.0.0",
    "pytest-asyncio>=0.24.0,<1.0.0",
    "ruff>=0.8.0,<1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agents", "apps/api/app", "shared"]

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["agents", "apps/api", "shared", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "ASYNC", "RUF"]

[tool.ruff.lint.isort]
known-first-party = ["app", "shared"]

[tool.black]
line-length = 100
target-version = ["py312"]
include = '\.pyi?$'

[tool.mypy]
python_version = "3.12"
files = ["agents", "apps/api", "shared", "tests"]
strict = true
warn_unreachable = true
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = ["tests.*"]
disallow_untyped_decorators = false

[tool.pytest.ini_options]
addopts = "-ra"
asyncio_mode = "auto"
testpaths = ["agents", "tests"]
pythonpath = [".", "apps/api"]
markers = [
    "integration: optional tests that execute local FFmpeg and ffprobe",
]
